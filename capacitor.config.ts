import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.intento3',
  appName: 'intento3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
